<%=form_for([@restaurant, @review]) do |f| %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.label :rating %>
  <%= f.number_field :rating, {max: 5, min: 0} %>

  <%= f.submit%>


  <!-- [...] all the fields -->
<% end %>
